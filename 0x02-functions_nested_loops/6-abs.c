#include "main.h"

/**
 *_abs - prints the absolute value of an integer
 * Return: - 0 (success)
 * @a: Parameter to be commuted.
 */

int _abs(int a)
{
	if (a >= 0)
		_putchar(a + '0');
	else
		_putchar(a + '0');

return (0);
}
